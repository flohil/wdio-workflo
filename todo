allure-commandline npm module: -> dist/bin folder überschreiben mit inhalt aus template folder

# all evaluations must take place in specs, because allure does not display errors with correct message?

# screenshots are sent as part of json and are not displayed -> nur bei broken wird screenshot geschickt, manuell überarbeiten?
json antwort bei broken -> screenshot rausziehen, failed: screenshot manuell machen

# type defs for jasmine custom message 2

# screenshot when expect fails -> take it already during verify, if all went good: attach, else: throw away